import { ContainerTitlePipe } from './container-title.pipe';

describe('ContainerTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainerTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
